# Shared dependency updates

## Reuse completed materializations

Connect atoms, retrieval units, evidence, and derived artifacts through scoped
dependency stamps and completed-work receipts.

```{code-block} console
python -m examples.quickstarts.dependency_updates
```

```{include} ../../../docs/dependency-updates.md
:start-line: 2
```
